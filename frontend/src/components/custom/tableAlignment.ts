/**
 * List standard: table cells are centered, like the Dispositivos screen.
 * Put it on `TableCell` (or on the wrapper of a custom cell); flex cells also
 * need `justify-center`.
 */
export const CENTERED_CELL_CLASS = "text-center";

services:
  mailpit:
    image: axllent/mailpit:v1.27.4
    profiles:
      - local-mail
    ports:
      - 127.0.0.1:${MAILPIT_UI_PORT:-8025}:8025
    networks:
      - app-network
    restart: unless-stopped

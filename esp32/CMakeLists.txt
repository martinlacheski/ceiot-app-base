# CMakeLists.txt para proyecto IOT ESP-IDF

cmake_minimum_required(VERSION 3.16)

# Incluir el build system de ESP-IDF
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

# Definir el proyecto
project(iot_device)
